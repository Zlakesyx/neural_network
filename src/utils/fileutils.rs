extern crate serde_yaml;


use std::{
    collections::HashMap,
    fs::File,
};


fn read_yaml_config(filename: String) {
    //let f = File::open(filename)?;
    //let d: String = serde_yaml::from_reader(f)?;
    //println!("Read YAML string: {}", d);
    //Ok(())
}
